# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Database Configuration (Required)
DATABASE_URL="mysql://user:password@host:port/database"
SINGLESTORE_USER="your_singlestore_user"
SINGLESTORE_PASS="your_singlestore_password"
SINGLESTORE_DB="your_database_name"
SINGLESTORE_HOST="your_singlestore_host"
SINGLESTORE_PORT="3306"

# Supabase PostgreSQL (Required)
POSTGRES_URL="postgresql://user:password@host:port/database"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host:port/database"

# Client Configuration (Required)
NEXT_PUBLIC_ACCOUNT_ID="2251799813685249"

# Google Maps (Required - validated by src/env.js)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your_google_maps_api_key"

# AWS Configuration (Optional - only required if using S3 for uploads)
# AWS_ACCESS_KEY_ID="your_aws_access_key"
# AWS_SECRET_ACCESS_KEY="your_aws_secret_key"
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="your_s3_bucket_name"

# Vercel Deployment (Required for generate-site script)
# Find these by running: vercel team ls
# VERCEL_TEAM_SLUG="your-vercel-team-slug"
# VERCEL_ORG_ID="team_xxxxxxxxxxxxx"

# Environment
NODE_ENV="development"
